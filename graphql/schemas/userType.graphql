type User {
  id: ID!
  name: String
  position: String
  phone: String
  address: String
  email: String
  github: String
  linkedin: String
  bio: String
}

type Query {
  user(id: ID!): User,
  users: [User],
}